rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All Firestore reads and writes go through Cloud Functions (admin SDK).
    // No direct client access is permitted.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
